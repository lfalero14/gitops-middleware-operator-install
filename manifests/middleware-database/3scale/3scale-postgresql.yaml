kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: operator-3scale-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp3-csi #change
  volumeMode: Filesystem
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: operator-3scale-db
spec:
  serviceName: operator-3scale-db-service
  selector:
    matchLabels:
      app: operator-3scale-db
  replicas: 1
  template:
    metadata:
      labels:
        app: operator-3scale-db
    spec:
      containers:
        - name: operator-3scale-db
          image: docker.io/postgres:16.4
          volumeMounts:
            - mountPath: /data
              name: cache-volume
          env:
            - name: POSTGRES_USER
              value: admin3scale
            - name: POSTGRES_PASSWORD
              value: redhat
            - name: PGDATA
              value: /data/pgdata
            - name: POSTGRES_DB
              value: 3scaleBD
      volumes:
        - name: cache-volume
          persistentVolumeClaim:
            claimName: operator-3scale-storage
---
apiVersion: v1
kind: Service
metadata:
  name: operator-3scale-db
spec:
  selector:
    app: operator-3scale-db
  type: LoadBalancer
  ports:
    - port: 5432
      targetPort: 5432
